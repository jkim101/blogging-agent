Start the development server for the web dashboard.

```bash
python main.py serve
```

This starts FastAPI with uvicorn on http://localhost:8000 with auto-reload enabled.
Access the dashboard in your browser to manage pipelines and review content.
