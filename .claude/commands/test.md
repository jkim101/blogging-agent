Run the test suite.

```bash
pytest tests/ -v
```

For specific test files:
- `pytest tests/test_pipeline.py -v` — Pipeline logic tests
- `pytest tests/test_parsers.py -v` — Parser tests
- `pytest tests/test_agents.py -v` — Agent tests
